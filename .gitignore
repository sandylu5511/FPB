*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
*.jks
*.keystore
!debug.keystore
*.apk
# APK 签名校验块（与 .apk 同属构建产物）。注意 `*.apk` 拦不住 `x.apk.idsig`
# —— 后缀多了一截，匹配的是 `.idsig` 不是 `.apk`。
*.idsig

# Kotlin 构建产物（2.x 起独立于 .gradle/，不排会被 git add -A 带进去）
.kotlin/

# Python 工具脚本的字节码缓存（tools/ 与 tools/motion/ 下都会生成）
__pycache__/

# 重构前的源码快照（本地回滚用，含 M2 版本全文）
/backup/

/keys/
